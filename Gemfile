source 'https://rubygems.org'

gem 'rails', '4.2.3'                    # Bundle edge Rails instead: gem 'rails', github: 'rails/rails'


# views & assets
# ==============
gem 'sass-rails', '~> 5.0'              # Use SCSS for stylesheets
gem 'bootstrap-sass', '~> 3.3.5'        # Sass-powered version of Bootstrap
gem 'uglifier', '>= 1.3.0'              # Use Uglifier as compressor for JavaScript assets
gem 'coffee-rails', '~> 4.1.0'          # Use CoffeeScript for .coffee assets and views
gem 'jquery-rails'                      # Use jquery as the JavaScript library
gem 'turbolinks'                        # Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'jbuilder', '~> 2.0'                # Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder


# database
# ========
gem 'pg'                                # Use PostgreSQL as the database for Active Record

# system & support
# ================
# gem 'unicorn'                         # Use Unicorn as the app server
gem 'therubyracer', platforms: :ruby    # See https://github.com/rails/execjs#readme for more supported runtimes

# documentation
# =============
group :doc do
  gem 'sdoc', '~> 0.4.0'               # bundle exec rake doc:rails generates the API under doc/api.
end


# dev & test & debug
# ==================
group :development, :test do
  gem 'rspec-rails', '~> 3.0'           # Better safe than sorry!
  gem 'byebug'                          # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'web-console', '~> 2.0'           # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'spring'                          # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'faker'                           # Generates all kinds of useful stuff for testing, e.g. MAC addresses
  gem 'faker'                           # Generates all kinds of useful stuff for testing, e.g. IP/MAC addresses
  gem 'geocoder'                        # Geolocation solution for ruby
end





